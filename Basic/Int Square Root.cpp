#include<bits/stdc++.h>
using namespace std;
int main()
{
    int num,integer_square_root;
    cout<<"Enter a num : ";
    cin>>num;
    integer_square_root=sqrt(num);
    //or, integer_square_root=floor(sqrt(num));
    cout<<integer_square_root<<endl;
    return 0;
}

